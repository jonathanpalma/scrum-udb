import Operator from "interfaces/Operator";

const orderOperatorsByCategory = (operators: Array<Operator>): Array<Operator> => {

    //todo
    return operators;

}

export default orderOperatorsByCategory;